source "https://rubygems.org"

# GitHub Pages builds the site with exactly this gem, so local previews match production.
gem "github-pages", group: :jekyll_plugins

# Standard-library gems that newer Ruby versions no longer bundle by default.
gem "webrick"
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
